import React from 'react';
import Layout from '../components/layout/Layout';

function Shop(props) {
    return (
        <Layout>
            <h1>Shop</h1>
        </Layout>
    );
}

export default Shop;